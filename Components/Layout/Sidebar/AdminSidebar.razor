@inject NavigationManager Nav

<div class="admin-sidebar">
    <div class="admin-sidebar-top">
        <div class="admin-header">
        <!-- Title -->
            <h2>Adminpanel</h2>
            <p>Introduktionshantering</p>
        </div>

        <!-- Admin links -->
        <nav class="admin-nav">
            <a href="/admin" class="admin-link @(IsActive("/admin") ? "active" : "")">
                <span class="admin-icon">▣</span>
                <span>Översikt</span>
            </a>
            <a href="/admin/modules" class="admin-link @(IsActive("/admin/modules") ? "active" : "")">
                <span class="admin-icon">≣</span>
                <span>Moduler & innehåll</span>
            </a>
            <a href="/admin/users" class="admin-link @(IsActive("/admin/users") ? "active" : "")">
                <span class="admin-icon">◉</span>
                <span>Användarhantering</span>
            </a>
            <a href="/admin/quiz" class="admin-link @(IsActive("/admin/quiz") ? "active" : "")">
                <span class="admin-icon">?</span>
                <span>Quiz</span>
            </a>
        </nav>
    </div>

    <!-- Actions -->
    <div class="admin-sidebar-bottom">
        <button class="admin-btn back-btn" @onclick="GoBack">
            ← Avsluta admin
        </button>
        <button class="admin-btn logout-btn">
            Logga ut
        </button>
    </div>
</div>

@code {
    private bool IsActive(string route)
    {
        var currentUri = Nav.ToBaseRelativePath(Nav.Uri);
        return currentUri.Equals(route.TrimStart('/'), StringComparison.OrdinalIgnoreCase);
    }

    private void GoBack()
    {
        Nav.NavigateTo("/dashboard");
    }
}